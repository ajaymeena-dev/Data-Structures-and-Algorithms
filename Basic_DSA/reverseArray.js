function reverseArray(arr) {
  let n = arr.length;

  for (let i = arr.length - 1; i >= 0; i--) {
    console.log(arr[i]);
  }
}
const arr = [1, 11, 22, 2, 41, 5, 0, -1];
reverseArray(arr);
// console.log(result);
